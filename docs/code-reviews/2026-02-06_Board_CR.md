# 2026-02-06_Board_CR

## 修正重點

- 移除不須使用的變數、函式、重複功能的函式。
- 將其他檔案有關於 board 的函式修正。

## 問題發現

- `isInBoard` 不應放在 Board 裡面，嚴重影響程式邏輯。已修正。
- `isInBoard` 與 `isPositionValid` 功能重複，刪除 `isPositionValid`。
- 棋力與 PST 已是公開變數，不須做 `update` `get`。